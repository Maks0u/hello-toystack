services:
  nginx:
    build: ./nginx
    ports:
      - "80:80"
  server:
    build: ./server
    healthcheck:
      test: CMD ["npm", "test"]
      interval: 1m30s
      timeout: 30s
      retries: 5
      start_period: 30s
    depends_on:
      - nginx
